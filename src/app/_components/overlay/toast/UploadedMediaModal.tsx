'use client';
import { OverlayElementProps } from '@/lib/useOverlay/OverlayController';
import { motion } from 'framer-motion';

export default function UploadedMediaModal({ isOpen, close }: OverlayElementProps) {
  return <div></div>;
}
